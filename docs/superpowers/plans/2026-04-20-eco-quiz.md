# Eco Problems Quiz Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a separate light-themed quiz website about ecological problems with 12 hard questions, 20-second timer, single-select and multi-select tasks, shuffled question order, shuffled answers, scoring, and a results screen.

**Architecture:** Create a standalone `eco-quiz` folder with the same broad static-site shape as the existing project, but with separate assets and a different visual system. Keep quiz data, quiz rules, and browser rendering separated so shuffle logic, multi-select grading, and score calculation can be tested independently with dependency-free JScript tests via `cscript`.

**Tech Stack:** HTML5, CSS3, vanilla JavaScript, Windows Script Host JScript for automated tests, Python `http.server` for local preview

---

## File Map

- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\index.html`
  Responsibility: Standalone markup shell for the ecological quiz scenes.
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\style.css`
  Responsibility: `Nature Reborn` visual system, layout, answer states, light theme, and responsive behavior.
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\quiz-engine.js`
  Responsibility: Pure rules for shuffle, state transitions, scoring, timeout handling, single-select and multi-select evaluation.
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\quiz-data.js`
  Responsibility: 12 hard self-authored questions with categories, explanation text, image metadata, and answer type.
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\script.js`
  Responsibility: DOM rendering, timer, interactive selection, confirm button behavior, scene changes, and replay.
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`
  Responsibility: Dependency-free test harness covering shuffle, grading, scoring, and data integrity.

## Notes

- The existing car quiz remains untouched; all new files live inside `eco-quiz`.
- The `.pptx` is a theme/style reference only, not a question source.
- The project has no Node.js runtime, so automated tests must stay dependency-free and runnable with `cscript //nologo`.

### Task 1: Create the ecological quiz test harness

**Files:**
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`
- Test: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`

- [ ] **Step 1: Write the failing bootstrap test**

```javascript
var fso = new ActiveXObject("Scripting.FileSystemObject");

function loadScript(path) {
  var file = fso.OpenTextFile(path, 1);
  var source = file.ReadAll();
  file.Close();
  eval(source);
}

function assert(condition, message) {
  if (!condition) {
    throw new Error(message);
  }
}

var root = fso.GetAbsolutePathName("..");
loadScript(root + "\\quiz-engine.js");

assert(typeof EcoQuizEngine === "object", "EcoQuizEngine should be defined");
assert(typeof EcoQuizEngine.createSession === "function", "createSession should exist");

WScript.Echo("Eco engine bootstrap tests passed.");
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `cscript //nologo engine-tests.js`  
Expected: FAIL because `quiz-engine.js` does not exist yet.

- [ ] **Step 3: Write the minimal engine bootstrap**

```javascript
(function (root) {
  root.EcoQuizEngine = {
    createSession: function (questions) {
      return {
        questions: questions,
        currentIndex: 0,
        score: 0,
        correctCount: 0,
        status: "ready"
      };
    }
  };
})(this);
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `cscript //nologo engine-tests.js`  
Expected: PASS with `Eco engine bootstrap tests passed.`

### Task 2: Drive shuffle and grading rules with tests first

**Files:**
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\quiz-engine.js`
- Test: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`

- [ ] **Step 1: Add failing tests for shuffle, single-select, and multi-select**

```javascript
var sampleQuestions = [
  {
    type: "single",
    answers: [
      { text: "A", correct: false },
      { text: "B", correct: true },
      { text: "C", correct: false },
      { text: "D", correct: false }
    ]
  },
  {
    type: "multi",
    answers: [
      { text: "A", correct: true },
      { text: "B", correct: false },
      { text: "C", correct: true },
      { text: "D", correct: false }
    ]
  }
];

var prepared = EcoQuizEngine.prepareQuestions(sampleQuestions, 7);
assert(prepared.length === 2, "Prepared questions should keep all items");
assert(prepared[0].answers.length === 4, "Prepared questions should keep answers");

var singleResult = EcoQuizEngine.gradeAnswer(prepared[0], [prepared[0].answers[0].id]);
assert(typeof singleResult.isCorrect === "boolean", "Single-select grading should return a boolean result");

var multiSource = prepared[1];
var correctIds = [];
for (var i = 0; i < multiSource.answers.length; i++) {
  if (multiSource.answers[i].correct) correctIds.push(multiSource.answers[i].id);
}
var multiResult = EcoQuizEngine.gradeAnswer(multiSource, correctIds);
assert(multiResult.isCorrect === true, "Exact multi-select match should be correct");
assert(EcoQuizEngine.gradeAnswer(multiSource, [correctIds[0]]).isCorrect === false, "Partial multi-select answer should be wrong");
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `cscript //nologo engine-tests.js`  
Expected: FAIL because `prepareQuestions` and `gradeAnswer` do not exist yet.

- [ ] **Step 3: Write the minimal implementation**

```javascript
(function (root) {
  function makeRng(seed) {
    var value = seed || 1;
    return function () {
      value = (value * 1664525 + 1013904223) % 4294967296;
      return value / 4294967296;
    };
  }

  function shuffle(list, rng) {
    var copy = list.slice();
    for (var i = copy.length - 1; i > 0; i--) {
      var j = Math.floor(rng() * (i + 1));
      var temp = copy[i];
      copy[i] = copy[j];
      copy[j] = temp;
    }
    return copy;
  }

  function prepareQuestions(questions, seed) {
    var rng = makeRng(seed || 1);
    var prepared = [];

    for (var i = 0; i < questions.length; i++) {
      var source = questions[i];
      var answers = [];
      for (var j = 0; j < source.answers.length; j++) {
        answers.push({
          id: "q" + i + "-a" + j,
          text: source.answers[j].text,
          correct: source.answers[j].correct
        });
      }
      prepared.push({
        id: "q" + i,
        type: source.type,
        answers: shuffle(answers, rng)
      });
    }

    return shuffle(prepared, rng);
  }

  function gradeAnswer(question, selectedIds) {
    var chosen = {};
    var correctCount = 0;
    var chosenCorrectCount = 0;

    for (var i = 0; i < selectedIds.length; i++) {
      chosen[selectedIds[i]] = true;
    }

    for (var j = 0; j < question.answers.length; j++) {
      var answer = question.answers[j];
      if (answer.correct) {
        correctCount += 1;
      }
      if (answer.correct && chosen[answer.id]) {
        chosenCorrectCount += 1;
      }
      if (!answer.correct && chosen[answer.id]) {
        return { isCorrect: false };
      }
    }

    return {
      isCorrect: chosenCorrectCount === correctCount && selectedIds.length === correctCount
    };
  }

  root.EcoQuizEngine = {
    createSession: function (questions) {
      return {
        questions: questions,
        currentIndex: 0,
        score: 0,
        correctCount: 0,
        status: "ready"
      };
    },
    prepareQuestions: prepareQuestions,
    gradeAnswer: gradeAnswer
  };
})(this);
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `cscript //nologo engine-tests.js`  
Expected: PASS.

### Task 3: Add scoring, timeout, and progression tests

**Files:**
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\quiz-engine.js`
- Test: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`

- [ ] **Step 1: Add failing tests for score calculation and completion**

```javascript
var session = EcoQuizEngine.createSession(sampleQuestions);
var scoredSingle = EcoQuizEngine.applyAnswer(session, true, 16, "single");
assert(scoredSingle.score === 140, "Single correct answer should include base score and speed bonus");
assert(scoredSingle.correctCount === 1, "Correct answer should increment correctCount");

var scoredMulti = EcoQuizEngine.applyAnswer(session, true, 12, "multi");
assert(scoredMulti.score === 170, "Multi correct answer should score higher than single");

var timedOut = EcoQuizEngine.applyTimeout(session);
assert(timedOut.status === "timeout", "Timeout should set timeout status");

var nextSession = EcoQuizEngine.goNext({
  questions: sampleQuestions,
  currentIndex: 1,
  score: 210,
  correctCount: 2,
  status: "answered"
});
assert(nextSession.isComplete === true, "Last question should mark the quiz complete");
assert(typeof nextSession.resultTitle === "string", "Completed session should include a result title");
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `cscript //nologo engine-tests.js`  
Expected: FAIL because `applyAnswer`, `applyTimeout`, and `goNext` do not exist yet.

- [ ] **Step 3: Write the minimal implementation**

```javascript
function cloneSession(session) {
  return {
    questions: session.questions,
    currentIndex: session.currentIndex,
    score: session.score,
    correctCount: session.correctCount,
    status: session.status
  };
}

function getSpeedBonus(secondsLeft) {
  return Math.round((Math.max(0, Math.min(20, secondsLeft)) / 20) * 50);
}

function getBasePoints(type) {
  return type === "multi" ? 140 : 100;
}

function applyAnswer(session, isCorrect, secondsLeft, type) {
  var next = cloneSession(session);
  next.status = "answered";

  if (isCorrect) {
    next.correctCount += 1;
    next.score += getBasePoints(type) + getSpeedBonus(secondsLeft);
  }

  return next;
}

function applyTimeout(session) {
  var next = cloneSession(session);
  next.status = "timeout";
  return next;
}

function getResultTitle(correctCount) {
  if (correctCount >= 11) return "Адвокат планети";
  if (correctCount >= 8) return "Дослідник систем";
  if (correctCount >= 5) return "Екологічний аналітик";
  return "Спостерігач змін";
}

function goNext(session) {
  var next = cloneSession(session);
  if (session.currentIndex >= session.questions.length - 1) {
    next.isComplete = true;
    next.resultTitle = getResultTitle(session.correctCount);
    next.status = "complete";
    return next;
  }
  next.currentIndex += 1;
  next.status = "ready";
  return next;
}
```

Export the new functions in `EcoQuizEngine`.

- [ ] **Step 4: Run the test to verify it passes**

Run: `cscript //nologo engine-tests.js`  
Expected: PASS.

### Task 4: Add the 12-question dataset with integrity tests

**Files:**
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\quiz-data.js`
- Test: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`

- [ ] **Step 1: Add failing tests for data integrity**

```javascript
loadScript(root + "\\quiz-data.js");

assert(typeof EcoQuizQuestions !== "undefined", "EcoQuizQuestions should be defined");
assert(EcoQuizQuestions.length === 12, "There should be 12 ecological questions");

var singleCount = 0;
var multiCount = 0;

for (var i = 0; i < EcoQuizQuestions.length; i++) {
  var item = EcoQuizQuestions[i];
  assert(item.answers.length >= 4, "Each question should have at least 4 answers");
  assert(typeof item.image === "string" && item.image.length > 10, "Each question should include an image");
  assert(typeof item.fact === "string" && item.fact.length > 25, "Each question should include an explanation");
  assert(item.type === "single" || item.type === "multi", "Question type should be single or multi");
  if (item.type === "single") singleCount += 1;
  if (item.type === "multi") multiCount += 1;
}

assert(singleCount >= 6, "There should be a strong single-select set");
assert(multiCount >= 4, "There should be multiple multi-select questions");
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `cscript //nologo engine-tests.js`  
Expected: FAIL because `quiz-data.js` does not exist yet.

- [ ] **Step 3: Write the full dataset**

Create `quiz-data.js` with 12 self-authored hard ecological questions. Include:

- at least 6 single-select questions
- at least 4 multi-select questions
- categories such as `Мікропластик`, `Клімат`, `Вода`, `Енергетика`, `Відходи`, `Біорізноманіття`, `Швидка мода`, `Цифровий слід`
- image URLs suitable for a light poster-style layout
- explanations under `fact`

Each answer should use the shape:

```javascript
{
  text: "Answer text",
  correct: true
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `cscript //nologo engine-tests.js`  
Expected: PASS.

### Task 5: Build the separate site shell in `eco-quiz`

**Files:**
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\index.html`
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\script.js`

- [ ] **Step 1: Create the HTML shell**

```html
<main class="app-shell">
  <section class="scene scene--start is-visible" data-scene="start">...</section>
  <section class="scene scene--quiz" data-scene="quiz" hidden>...</section>
  <section class="scene scene--result" data-scene="result" hidden>...</section>
</main>
```

The start screen must call out `12 питань`, `20 секунд`, and `single + multi select`.

- [ ] **Step 2: Add DOM bindings in `script.js`**

```javascript
var dom = {
  startScene: document.querySelector('[data-scene="start"]'),
  quizScene: document.querySelector('[data-scene="quiz"]'),
  resultScene: document.querySelector('[data-scene="result"]'),
  startButton: document.getElementById("start-quiz"),
  restartButton: document.getElementById("restart-quiz"),
  questionCard: document.getElementById("question-card"),
  progressValue: document.getElementById("question-counter"),
  progressBar: document.getElementById("progress-bar"),
  scoreValue: document.getElementById("score-value"),
  timerValue: document.getElementById("time-left")
};
```

- [ ] **Step 3: Wire scene switching**

```javascript
function showScene(name) {
  [dom.startScene, dom.quizScene, dom.resultScene].forEach(function (scene) {
    var visible = scene.getAttribute("data-scene") === name;
    scene.hidden = !visible;
    scene.classList.toggle("is-visible", visible);
  });
}
```

- [ ] **Step 4: Verify the shell can be served**

Run: `python -m http.server 8000` from `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz`  
Expected: local server starts without errors.

### Task 6: Render single-select and multi-select questions

**Files:**
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\script.js`
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\index.html`

- [ ] **Step 1: Render prepared questions**

Use `EcoQuizEngine.prepareQuestions(EcoQuizQuestions, Date.now())` at session start so each run shuffles both questions and answers.

- [ ] **Step 2: Add question rendering with answer IDs**

Render answer buttons using answer IDs from the prepared data, not original indices.

- [ ] **Step 3: Support both interaction modes**

For `single`:

```javascript
function handleSingleSelect(answerId) {
  submitAnswer([answerId]);
}
```

For `multi`:

```javascript
function toggleMultiSelect(answerId) {
  if (selectedMap[answerId]) {
    delete selectedMap[answerId];
  } else {
    selectedMap[answerId] = true;
  }
  renderSelectionState();
}
```

Require a dedicated `Підтвердити` button for `multi`.

- [ ] **Step 4: Verify the interaction path manually**

Run: `python -m http.server 8000`  
Expected: both question types render and remain usable.

### Task 7: Implement timer, scoring, explanations, and result screen

**Files:**
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\script.js`
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\quiz-engine.js`

- [ ] **Step 1: Add the 20-second timer flow**

The timer should:

- start on each new question
- update the numeric counter and progress indicator
- auto-submit timeout when it reaches `0`

- [ ] **Step 2: Add answer submission**

Submission must:

- grade using `EcoQuizEngine.gradeAnswer(...)`
- apply score with `EcoQuizEngine.applyAnswer(...)`
- show explanation text
- lock the question UI
- enable `Далі`

- [ ] **Step 3: Add result rendering**

Result screen must show:

- total score
- correct count
- accuracy percentage
- result title from `EcoQuizEngine.getResultTitle(...)`

- [ ] **Step 4: Verify the flow manually**

Run: `python -m http.server 8000`  
Expected: full quiz path works start -> question loop -> result -> replay.

### Task 8: Build the new `Nature Reborn` design and responsive polish

**Files:**
- Create: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\style.css`
- Modify: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\index.html`

- [ ] **Step 1: Define the light poster-like palette**

Use CSS custom properties for warm white, sand, sage, leaf green, turquoise, and coral accents.

- [ ] **Step 2: Style the scenes and cards**

The quiz must visually differ from the car quiz:

- light backgrounds
- soft shadows
- poster-like panels
- organic accents
- different typography

- [ ] **Step 3: Add answer state styling**

Provide clear states for:

- selectable answers
- selected multi-select answers
- correct answers
- wrong answers
- disabled answers

- [ ] **Step 4: Add responsive rules**

Ensure:

- no horizontal scroll
- mobile-friendly stacked layout
- readable text over images
- large tappable controls

### Task 9: Final verification

**Files:**
- Review: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\index.html`
- Review: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\style.css`
- Review: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\quiz-data.js`
- Review: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\quiz-engine.js`
- Review: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\script.js`
- Review: `C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz\tests\engine-tests.js`

- [ ] **Step 1: Run engine tests**

Run: `cscript //nologo tests\engine-tests.js`  
Expected: PASS.

- [ ] **Step 2: Run a local preview**

Run: `python -m http.server 8000`  
Expected: `http://localhost:8000/` loads the ecological quiz.

- [ ] **Step 3: Check acceptance points**

```text
- New separate site exists in eco-quiz
- 12 questions exist
- Both single-select and multi-select exist
- Question order changes between runs
- Answer order changes between runs
- 20-second timer works
- Score updates correctly
- Results screen shows score, accuracy, and title
- Design is clearly different from the car quiz
```

- [ ] **Step 4: Share run instructions**

```text
1. cd "C:\Users\ilyax\OneDrive\Desktop\Нова папка\eco-quiz"
2. python -m http.server 8000
3. open http://localhost:8000/
```
